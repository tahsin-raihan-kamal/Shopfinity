# Shopfinity — Web (Next.js)

This folder contains the storefront and admin UI.

Setup, scripts, and architecture are documented in the [**repository root README**](../README.md).

```bash
cd shopfinity-web
cp .env.example .env.local   # Windows: copy .env.example .env.local
npm install
npm run dev
```
